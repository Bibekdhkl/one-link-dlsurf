version: '3.8'
services:
  app:
    build: .
    ports:
      - "2000:2000"
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
    # Uncomment below if you want to mount code for live reload (dev only)
    # volumes:
    #   - .:/app
